# Base URL the frontend uses for API calls. In Docker/production this is
# proxied through Nginx; in local dev, Vite's dev-server proxy (vite.config.ts)
# forwards /api to the backend, so this can usually stay as-is.
VITE_API_BASE_URL=/api/v1
